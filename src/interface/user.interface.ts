export interface User {
    id?: string;
    firstName: string;
    lastName: string;
    email: string;
    birthDate: number | string;
    street: string;
    zipCode: number | string;
    city: string;
}